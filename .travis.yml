language: python

python:
  - "2.7"
  - "3.5"

env:
  - DJANGO_VERSION=1.9.9
  - DJANGO_VERSION=1.10
  
before_install:
  - . $HOME/.nvm/nvm.sh
  - nvm install stable
  - nvm use stable
  - npm install mjml

install:
  - pip install -q Django==$DJANGO_VERSION

script:
  - python tools.py test
